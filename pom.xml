<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>br.com.fiap</groupId>
    <artifactId>clyvovet</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>clyvovet</name>
    <description>clyvovet</description>
    <url/>
    <licenses>
        <license/>
    </licenses>
    <developers>
        <developer/>
    </developers>
    <scm>
        <connection/>
        <developerConnection/>
        <tag/>
        <url/>
    </scm>
    <properties>
        <java.version>17</java.version>
        <jjwt.version>0.12.6</jjwt.version>
        <bucket4j.version>8.10.1</bucket4j.version>

        <!--
          CORRECAO DE DEPENDENCIA VULNERAVEL (achado 2.9 da auditoria)

          As quatro propriedades abaixo sobrepoem o que o spring-boot-dependencies
          3.5.16 fixa. Nao ha patch do Boot para subir: 3.5.16 E a ultima versao
          publicada, e ela fixa justamente as versoes com aviso.

          Levantado consultando a API publica do OSV (api.osv.dev/v1/querybatch)
          com as 110 dependencias de runtime reais do projeto. O
          owasp:dependency-check nao serviu aqui porque exige chave da NVD.

          CRITERIO DA VERSAO ESCOLHIDA: a MENOR versao que corrige, e nao a mais
          recente. O spring-boot-dependencies e um conjunto curado e testado junto;
          quanto menor o desvio dele, menor a chance de trocar uma vulnerabilidade
          por uma incompatibilidade. Cada linha diz o aviso que fecha.
        -->

        <!--
          TRES AVISOS CRITICOS, e este e o mais grave dos quatro: o tomcat embutido
          E a camada HTTP da aplicacao, exposta na internet no App Service.

            GHSA-9xv2-5v5q-p794  bypass de autenticacao por captura-replay (DIGEST)
            GHSA-gcx9-497g-6cp6  controle de acesso e autorizacao incorretos
            GHSA-h3x4-894j-xpx5  autorizacao incorreta na autenticacao FORM

          Corrigidos na 10.1.58, que NUNCA foi publicada no Maven Central — o
          Tomcat pula versoes. A 10.1.59 e a seguinte e ja contem as correcoes.

          Fica na linha 10.1 de proposito. O Tomcat 11 e Servlet 6.1 / Jakarta EE
          11, e o Spring Boot 3.5 exige Servlet 6.0 / EE 10: subir para 11.x nao e
          atualizar, e trocar de plataforma.
        -->
        <tomcat.version>10.1.59</tomcat.version>

        <!--
          GHSA-5gvw-p9qm-jgwh, GHSA-5jmj-h7xm-6q6v, GHSA-mhm7-754m-9p8w (moderados)
          Tres formas de contornar @JsonView e @JsonIgnoreProperties na
          desserializacao. Importa aqui porque toda entrada da API passa pelo
          Jackson, e os DTOs de Request sao a fronteira de validacao.
        -->
        <jackson-bom.version>2.21.5</jackson-bom.version>

        <!--
          GHSA-qv9r-c865-cp47 (moderado) — codificacao indevida de valores
          float nao-finitos na serializacao JSON de MapMessage.
          A propriedade controla log4j-api e log4j-to-slf4j juntos, entao os dois
          sobem em sincronia. O projeto loga por logback; o log4j entra so como
          ponte, o que mantem a exposicao pequena.
        -->
        <log4j2.version>2.25.5</log4j2.version>

        <!--
          GHSA-j288-q9x7-2f5v (moderado) — recursao sem controle ao processar
          entradas longas.
        -->
        <commons-lang3.version>3.18.0</commons-lang3.version>
    </properties>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-cache</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Controle de versao do schema -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-oracle</artifactId>
        </dependency>
        <!-- O Flyway 10+ separa o suporte a cada banco em um modulo proprio.
             Os dois convivem: o Oracle continua servindo de banco de testes e
             o MySQL e o alvo do deploy. Qual conjunto de migrations roda e
             decidido pelo perfil, em spring.flyway.locations. -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
        </dependency>

        <!-- Geracao e validacao de JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Rate limiting -->
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j-core</artifactId>
            <version>${bucket4j.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!--
            HATEOAS: leva a API do nivel 2 ao nivel 3 de Richardson.

            O cliente deixa de precisar saber, por fora, o que pode fazer com um
            recurso: a propria resposta carrega os links das transicoes
            possiveis NAQUELE estado. Um evento AGENDADO traz "cancelar" e
            "concluir"; o mesmo evento, depois de CANCELADO, nao traz nenhum dos
            dois. E a diferenca entre documentar a maquina de estados e
            expressa-la.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-hateoas</artifactId>
        </dependency>

        <!--
            Actuator: a probe de saude que o Azure Container Apps consulta para
            saber se a instancia pode receber trafego. So /health fica exposto —
            os demais endpoints publicam versao, beans e configuracao.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.8.16</version>
        </dependency>

        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>com.oracle.database.jdbc</groupId>
            <artifactId>ojdbc11</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <executions>
                    <execution>
                        <id>default-compile</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <configuration>
                            <annotationProcessorPaths>
                                <path>
                                    <groupId>org.projectlombok</groupId>
                                    <artifactId>lombok</artifactId>
                                </path>
                            </annotationProcessorPaths>
                        </configuration>
                    </execution>
                    <execution>
                        <id>default-testCompile</id>
                        <phase>test-compile</phase>
                        <goals>
                            <goal>testCompile</goal>
                        </goals>
                        <configuration>
                            <annotationProcessorPaths>
                                <path>
                                    <groupId>org.projectlombok</groupId>
                                    <artifactId>lombok</artifactId>
                                </path>
                            </annotationProcessorPaths>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
