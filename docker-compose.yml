services:
  clyvovet-db:
    image: oscarfonts/h2
    container_name: clyvovet-db
    ports:
      - "1521:1521"
      - "81:81"
    environment:
      - H2_OPTIONS=-ifNotExists
    volumes:
      - clyvovet-h2-data:/opt/h2-data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://localhost:81"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 20s

  clyvovet-api:
    build: .
    container_name: clyvovet-api
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=h2
      # Defina JWT_SECRET no ambiente (ou num .env fora do versionamento).
      # O default abaixo serve apenas para subir o compose localmente.
      - JWT_SECRET=${JWT_SECRET:-ZGVzZW52b2x2aW1lbnRvLWNseXZvdmV0LWNoYXZlLWhtYWMtc2hhMjU2LWxvY2Fs}
    depends_on:
      clyvovet-db:
        condition: service_healthy
    restart: unless-stopped

volumes:
  clyvovet-h2-data:
    name: clyvovet-h2-data