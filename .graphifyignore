# O grafo descreve a aplicacao ClyvoVet. Ficam de fora os arquivos que so
# falam de ferramentas:
#   - as instrucoes da skill do graphify (60+ nos sobre o proprio graphify);
#   - a memoria de trabalho de outro assistente;
#   - o utilitario que conserta os nomes das comunidades -- a ferramenta que
#     arruma o grafo nao deve virar uma comunidade dentro dele.
.claude/skills/
.junie/
scripts/
.claude/settings*.json
